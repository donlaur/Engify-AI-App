# Super Admin Setup Configuration
# Copy this file to .env.local and fill in the values

# MongoDB connection string (REQUIRED)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/engify

# Super Admin Credentials
SUPER_ADMIN_EMAIL=admin@engify.ai
SUPER_ADMIN_PASSWORD=ChangeMe123!SecureP@ssw0rd
SUPER_ADMIN_NAME=Super Admin

# Usage:
# 1. Copy this file: cp .env.admin.example .env.local
# 2. Edit .env.local with your actual credentials
# 3. Run: pnpm admin:setup
#
# Password Requirements:
# - Minimum 12 characters
# - At least one uppercase letter
# - At least one lowercase letter
# - At least one number
# - At least one special character
#
# Security Notes:
# - NEVER commit .env.local to version control
# - Use a password manager to generate strong passwords
# - Change default passwords immediately after setup
# - Enable MFA after first login
